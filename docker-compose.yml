services:
  api-lite:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./backend:/app/data
    environment:
      - GIN_MODE=release
      - CONFIG_PATH=/app/data/config.yaml
